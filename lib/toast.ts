type ToastKind = 'success' | 'error' | 'loading';

export function showToast(_message: string, _kind: ToastKind = 'success') {
  // Native placeholder. We can swap this to a native toast lib anytime.
}

